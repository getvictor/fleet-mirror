- Added support for hosts running AMD Ryzen AI Developer Platform, a Debian-based Linux distribution that reports `platform=amd-ryzen-ai-developer-platform`. Fleet now recognizes these hosts as Linux, so host vitals (disk space, IP and MAC addresses), local user accounts, and software inventory populate, policies and labels scoped to `linux` apply, and scripts can be run from the host's Actions menu.
